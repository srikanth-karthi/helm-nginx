replicaCount: 2

image:
  repository: nginx
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

config:
  homepage: "<h1>Welcome to My WebApp!</h1><p>Deployed via Helm 🚀</p>"
